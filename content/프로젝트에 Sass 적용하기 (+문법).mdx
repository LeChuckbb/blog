---
tags:
  - CSS
  - Sass
slug: sass
description: null
date: '2022-02-27'
title: 프로젝트에 Sass 적용하기 (+문법)
---
import {Fragment as _Fragment, jsx as _jsx, jsxs as _jsxs} from "react/jsx-runtime";
function _createMdxContent(props) {
  const _components = {
    a: "a",
    blockquote: "blockquote",
    code: "code",
    h2: "h2",
    h3: "h3",
    img: "img",
    p: "p",
    pre: "pre",
    ...props.components
  };
  return _jsxs(_Fragment, {
    children: [_jsx(_components.p, {
      children: "Sass는 CSS 전처리기(preprocessor)로 CSS 사용에 편의를 돕는다. CSS 전처리기는 전처리기 자신만의 특별한 문법을 갖고, 추후에 css로 변환(생성)해서 사용하는 도구를 뜻한다."
    }), "\n", _jsxs(_components.blockquote, {
      children: ["\n", _jsx(_components.p, {
        children: "“Prepro-what?\" Think of CSS Preprocessors as programs that help you generate CSS using a unique syntax."
      }), "\n"]
    }), "\n", _jsx(_components.p, {
      children: "Sass의 종류로는 Ruby Sass, LibSass(node-sass), Dart Sass가 있는데, 현재는 Dart Sass만 공식적으로 update되고 있는 것 같다."
    }), "\n", _jsxs(_components.p, {
      children: [_jsx(_components.code, {
        children: "npm i -g sass"
      }), "로 sass를 설치하여 사용한다."]
    }), "\n", _jsx(_components.h2, {
      children: "Sass를 CSS로 컴파일하기"
    }), "\n", _jsx(_components.h3, {
      children: "사용법 1. css로 변환해서 사용하기"
    }), "\n", _jsxs(_components.p, {
      children: ["command line에서 ", _jsx(_components.code, {
        children: "sass (.scss file path) (.css file path)"
      }), " 명령어를 이용해서 작성한 scss 파일을 css파일로 컴파일하고, 컴파일 된 css파일을 HTML 파일에서 ", _jsx(_components.code, {
        children: "<link>"
      }), "해 사용한다."]
    }), "\n", _jsxs(_components.p, {
      children: ["--watch flag를 이용해서 좀 더 간편하게 이용할 수 있다. 이는 소스코드에 변화가 생긴 sass 파일을 추적하여 자동으로 css 파일로 컴파일해 줄 것이다. ", _jsx(_components.code, {
        children: "sass --watch (.scss file path) (.css file path)"
      }), "와 같이 입력하면 .scss파일이 변경될 때마다 자동으로 컴파일하여 css 파일을 만들어낸다. 파일 단위가 아닌 폴더 단위를 감시하려면 ", _jsx(_components.code, {
        children: "sass --watch (.scss folder path):(.css folder path)"
      }), "와 같이 입력한다."]
    }), "\n", _jsxs(_components.blockquote, {
      children: ["\n", _jsx(_components.p, {
        children: "vs code의 Liver Server + Sass --watch flag를 활용하면 편리하다."
      }), "\n"]
    }), "\n", _jsx(_components.h3, {
      children: "사용법 2. bundler로 css 변환 및 빌드 과정을 자동화하기"
    }), "\n", _jsx(_components.p, {
      children: "모듈 = 분리된 코드의 조각. 시스템을 이루는 논리적인 일부분."
    }), "\n", _jsx(_components.p, {
      children: "모듈 번들러 = 여러 모듈을 모아 번들링해서 하나의 파일로 단일화."
    }), "\n", _jsx(_components.p, {
      children: "모듈 번들러의 종류로는 웹팩, parcel 등이 있다."
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.code, {
        children: "npm i parcel"
      })
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.code, {
        children: "npx parcel index.html"
      })
    }), "\n", _jsxs(_components.blockquote, {
      children: ["\n", _jsx(_components.p, {
        children: "parcel-bundler 는 구버전(parcel 1.x)이다!"
      }), "\n"]
    }), "\n", _jsx(_components.h2, {
      children: "Sass 문법"
    }), "\n", _jsx(_components.h3, {
      children: "Variables"
    }), "\n", _jsxs(_components.p, {
      children: ["Sass에서 변수는 ", _jsx(_components.code, {
        children: "$"
      }), "을 붙여서 선언한다. Sass 변수에는 style rule, at-rule,"]
    }), "\n", _jsxs(_components.p, {
      children: [_jsx(_components.code, {
        children: "!default"
      }), " flag를 통해서 해당 변수가 초기화되지 않았거나 null로 초기화되어 있을 경우에만 값을 할당할 수 있다. 해당 변수가 이미 다른 값으로 초기화되어 있다면, 할당문을 무시하고 그 값을 사용할 것이다."]
    }), "\n", _jsxs(_components.blockquote, {
      children: ["\n", _jsx(_components.p, {
        children: "Sass에서 $-와 같은 형태로 시작하는 변수는 private 변수다."
      }), "\n"]
    }), "\n", _jsx(_components.h3, {
      children: "Nesting"
    }), "\n", _jsx(_components.h3, {
      children: "Partials"
    }), "\n", _jsxs(_components.p, {
      children: ["Sass 파일을 여러 개의 파일로 쪼개서 모듈화하기. 파일명 앞에 ", _jsx(_components.code, {
        children: "_"
      }), "표시를 하여 partial 파일임을 알린다. ", _jsx(_components.code, {
        children: "_"
      }), "표시는 Sass로 하여금 이 파일이 css로 컴파일할 대상이 아닌, 다른 scss 파일의 partial 파일임을 상기시킨다. partials는 ", _jsx(_components.code, {
        children: "@use"
      }), " 규칙과 함께 사용된다."]
    }), "\n", _jsx(_components.h3, {
      children: "Modules"
    }), "\n", _jsxs(_components.p, {
      children: [_jsx(_components.code, {
        children: "@use"
      }), " 규칙을 이용하면 하나의 Sass 파일에 모든 코드를 작성할 필요가 없다. 이 룰은 다른 Sass 파일을 module로써 load한다. 불러온 파일은 파일명을 기반으로 한 네임스페이스의 변수,믹스인,함수를 포함한다."]
    }), "\n", _jsxs(_components.p, {
      children: [_jsx(_components.code, {
        children: "@use"
      }), " 규칙은 다른 Sass stylesheets로부터 mixins, functions, variables를 가져오는 규칙이다. 이렇게 다른 styleshhet로부터 가져온 것들을 'modules'라고 부른다."]
    }), "\n", _jsxs(_components.p, {
      children: [_jsx(_components.code, {
        children: "@use"
      }), "로 불러온 modules는 ", _jsx(_components.code, {
        children: "(namespace).(variable)"
      }), "과 같이 namespace를 붙인 형태로 사용할 수 있다. ", _jsx(_components.code, {
        children: "@use (fileurl) as (namespace or *)"
      }), "과 같은 형태로 불러올 때 namespace를 짧게 지정할 수도 있고, ", _jsx(_components.code, {
        children: "*"
      }), "은 namespace 없이 로드하겠다는 의미다. 즉, namespace를 생략하고 ", _jsx(_components.code, {
        children: "(variable)"
      }), "로 modules를 바로 사용할 수 있다."]
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.img, {
        src: "https://images.velog.io/images/lechuck/post/45a62a8f-9e79-42d8-8694-e7ce3dde0cff/image.png",
        alt: ""
      })
    }), "\n", _jsx(_components.h3, {
      children: "@use + @forward"
    }), "\n", _jsxs(_components.p, {
      children: [_jsx(_components.code, {
        children: "@forward"
      }), "는 ", _jsx(_components.code, {
        children: "@use"
      }), "의 반복적인 사용을 줄여 편리하게 해준다. @forward는 @use 규칙을 사용하여 load된 stylesheets의 members를 public하게 드러낸다."]
    }), "\n", _jsxs(_components.p, {
      children: ["7-1패턴에서 7개 폴더에 각각의 _index.scss 파일(entrypoint file)을 만들고 이 index 파일에서 해당 폴더의 모든 모듈들을 ", _jsx(_components.code, {
        children: "@forward"
      }), "해 두면, 다른 폴더의 모듈에서 특정 폴더의 모듈을 참조할 때 해당 폴더의 모든 모듈을 ", _jsx(_components.code, {
        children: "@use"
      }), "하는 게 아니라 인덱스 파일만 ", _jsx(_components.code, {
        children: "@use"
      }), "해서 간편하게 사용할 수 있다."]
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.a, {
        href: "https://www.youtube.com/watch?v=CR-a8upNjJ0&t=699s&ab_channel=KevinPowell",
        children: "Stop using @import with Sass | @use and @forward explained"
      })
    }), "\n", _jsx(_components.h3, {
      children: "@use vs @import"
    }), "\n", _jsxs(_components.p, {
      children: ["Sass는 CSS의 ", _jsx(_components.code, {
        children: "@import"
      }), " 문법과 유사하지만 독자적인 ", _jsx(_components.code, {
        children: "@import"
      }), " 문법을 갖고 있다. 하지만 Sass는 ", _jsx(_components.code, {
        children: "Sass-@import"
      }), "마저 사용을 지양하는 추세다. ", _jsx(_components.code, {
        children: "@import"
      }), "는 모든 variables, mixins, functions에  global scope를 형성하여 naming collision이 발생할 여지를 주고, @use에 비하면 컴파일 시간도 길다."]
    }), "\n", _jsxs(_components.p, {
      children: ["Sass는 @import의 좋은 대안인 @use를 권장한다. @use는 ", _jsx(_components.code, {
        children: "namespace.$변수명"
      }), " 문법을 통해서 naming collision 문제를 방지하기도 한다."]
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.a, {
        href: "https://sass-lang.com/documentation/at-rules/import",
        children: "Sass At-Rules @import"
      })
    }), "\n", _jsx(_components.h3, {
      children: "Mixins"
    }), "\n", _jsx(_components.p, {
      children: "mixin은 일종의 CSS 선언 그룹이다. 이 그룹으로 코드를 재활용 할 수 있다. mixin에는 값을 전달하여 함수처럼 활용할 수 있다."
    }), "\n", _jsxs(_components.p, {
      children: ["mixin은 다음과 같이 정의한다. ", _jsx(_components.code, {
        children: "@mixin 이름(매개변수)"
      }), "\n정의한 mixin은 ", _jsx(_components.code, {
        children: "@include 이름(매개변수)"
      }), "과 같은 형태로 사용한다."]
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.img, {
        src: "https://images.velog.io/images/lechuck/post/c558ed77-d31d-47a2-b816-99d130b05c30/image.png",
        alt: ""
      })
    }), "\n", _jsx(_components.h3, {
      children: "Extend/Inheritance"
    }), "\n", _jsx(_components.p, {
      children: "이 둘은 코드 재사용의 목적으로 혼용되어 쓰이고 있지만, 구분해서 사용할 필요가 있다."
    }), "\n", _jsxs(_components.p, {
      children: [_jsx(_components.code, {
        children: "@extend"
      }), "를 사용하여 css 덩어리를 재사용할 수 있다. ", _jsx(_components.code, {
        children: "%"
      }), "로 정의된 클래스는 placeholder class라 불리며, ", _jsx(_components.code, {
        children: "@extend"
      }), "로 상속하기 이전까지는 css 스타일 적용이 유보된다. 즉, placeholder class를 정의해놓고 ", _jsx(_components.code, {
        children: "@extend"
      }), " 키워드를 통해 선택적으로 css 스타일을 적용할 수 있다."]
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.img, {
        src: "https://images.velog.io/images/lechuck/post/32b18d8f-33ee-4ece-ad15-3a81ed58717c/image.png",
        alt: ""
      })
    }), "\n", _jsx(_components.h3, {
      children: "Extend(상속)과 mixin의 차이"
    }), "\n", _jsx(_components.p, {
      children: "Mixin은 소스코드의 중복을 막기 위해서 사용된다면, extend(상속)는 연관성있는 규칙을 만들기 위해 사용한다. 이 둘의 차이는 css로 컴파일 되었을 때 속성들이 한 곳에 묶이느냐 안 묶이느냐에 차이가 있다."
    }), "\n", _jsxs(_components.p, {
      children: ["아래 코드를 보면 서로 연관성이 없는 세 개의 클래스 .main_container, .view_container, .detail_container에 코드를 재사용할 목적으로 ", _jsx(_components.code, {
        children: "@extend"
      }), "를 활용한 모습이다. 이럴때는 ", _jsx(_components.code, {
        children: "@mixin"
      }), "을 사용하는 것이 맞다. css로 변환된 ", _jsx(_components.code, {
        children: "@extend"
      }), "(상속) 문법의 코드를 보면 마치 연관성이 있는 것 마냥 묶이기 때문이다."]
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.img, {
        src: "https://images.velog.io/images/lechuck/post/fb7d7695-3430-45ca-a4fe-08624efd2d7f/image.png",
        alt: ""
      })
    }), "\n", _jsxs(_components.p, {
      children: ["아래는 ", _jsx(_components.code, {
        children: "@mixin"
      }), "을 적용해본 모습이다. 세 개의 연관성 없는 콘테이너는 아래와 같이 따로 작성되는 것이 맞다. 연관성은 없지만 코드의 재사용을 피하고 싶을 때 mixin을 활용한다. 연관성 있는 것들을 묶을 때(재사용도 줄이면서) extend를 활용한다."]
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.img, {
        src: "https://images.velog.io/images/lechuck/post/4cb4e3d8-4732-40de-89fa-561254755358/image.png",
        alt: ""
      })
    }), "\n", _jsx(_components.h3, {
      children: "Operators"
    }), "\n", _jsxs(_components.p, {
      children: ["Sass는 ", _jsx(_components.code, {
        children: "+, -, *, math.div(), %"
      }), "와 같은 수학 연산들을 적용할 수 있다."]
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.img, {
        src: "https://images.velog.io/images/lechuck/post/869acd5a-7c22-482b-a417-9e18d4546745/image.png",
        alt: ""
      })
    }), "\n", _jsx(_components.h2, {
      children: "Sass + BEM"
    }), "\n", _jsx(_components.p, {
      children: "Sass(2006)는 BEM(2013) 이전에 탄생했다. 따라서 Sass의 Nesting 문법을 남용하면 Specificity(명시도) 관리에 애를 먹게 되는데, Sass의 편리한 문법은 취하면서도 BEM을 준수하여 Specificity를 준수할 필요가 있다."
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.img, {
        src: "https://images.velog.io/images/lechuck/post/9aede285-0057-4513-be8a-824f4ae3da4c/image.png",
        alt: ""
      })
    }), "\n", _jsx(_components.p, {
      children: "What we’ve done here is call out the .nav as the Block. Anything with double underscores __ are our Elements within that Block. And anything with double dash -- are our Modifiers."
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.img, {
        src: "https://images.velog.io/images/lechuck/post/7d0278a9-f1fd-414e-bb1f-5ba1d6e6ee32/image.png",
        alt: ""
      })
    }), "\n", _jsx(_components.p, {
      children: "The & essentially saves us rewriting the parent selector over and over."
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.img, {
        src: "https://images.velog.io/images/lechuck/post/eb5457bf-bdd0-48c5-80d7-d4b9de342085/image.png",
        alt: ""
      })
    }), "\n", _jsx(_components.p, {
      children: "Here we have a block called \"bar\". It contains an image “bar__logo” and a list “bar__list” of links “bar__item” and “link” (which is another small block used throughout the project). For one of the links, we also have a modifier, \"--highlight\", which points out that our link will look different from the others. That’s it!"
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.img, {
        src: "https://images.velog.io/images/lechuck/post/072ac203-11a3-40dd-966e-6769665dc971/image.png",
        alt: ""
      })
    }), "\n", _jsxs(_components.p, {
      children: ["exam 3)\n", _jsx(_components.img, {
        src: "https://images.velog.io/images/lechuck/post/f5b05802-f61f-46f7-9d77-e3b1b639fab5/image.png",
        alt: ""
      })]
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.img, {
        src: "https://images.velog.io/images/lechuck/post/458dbc5f-196d-48f1-8307-ce59b92412bc/image.png",
        alt: ""
      })
    }), "\n", _jsx(_components.h2, {
      children: "Sass 7-1 Architecture"
    }), "\n", _jsx(_components.p, {
      children: "7-1 패턴은 7개의 폴더와 1개의 파일로 이루어진 구조를 뜻한다."
    }), "\n", _jsx(_components.pre, {
      children: _jsx(_components.code, {
        className: "language-dir",
        children: "sass/\n|\n|– abstracts/\n|   // abstract 폴더의 style들은 CSS로 컴파일되지 않고,\n|   // 다른 폴더를 돕는 helper다.\n|   // 추후 프로젝트에 변경사항이 있을 때 abstract 폴더의 값만을 수정하여 관리할 수 있다.\n|   |– _variables.scss    # Sass Variables\n|   |– _functions.scss    # Sass Functions\n|   |– _mixins.scss       # Sass Mixins + media query\n|   |– _placeholders.scss # Sass Placeholders\n|\n|– base/\n|\t// 프로젝트를 위한 boilerplate code가 담긴 폴더\n|\t|- _base.scss         # initialize styles\n|   |– _reset.scss        # Reset/normalize\n|   |– _typography.scss   # Typography rules\n|   …                     # Etc.\n|\n|– components/\n|   // Components are a set of codes which has their character. \n|   // It might be just one HTML element or a block of HTML elements.\n|   |– _buttons.scss      # Buttons\n|   |– _carousel.scss     # Carousel\n|   |– _cover.scss        # Cover\n|   |– _dropdown.scss     # Dropdown\n|   …                     # Etc.\n|\n|– layout/\n|\t// 웹사이트 레이아웃에 관련된 파일들이 담긴 폴더\n|   |– _navigation.scss   # Navigation\n|   |– _grid.scss         # Grid system\n|   |– _header.scss       # Header\n|   |– _footer.scss       # Footer\n|   |– _sidebar.scss      # Sidebar\n|   |– _forms.scss        # Forms\n|   …                     # Etc.\n|\n|– pages/\n|   // 해당 페이지를 대상으로한 style.\n|   // e-commerce project에 주로 쓰임. _login.scss _signup.scss\n|   |– _home.scss         # Home specific styles\n|   |– _contact.scss      # Contact specific styles\n|   …                     # Etc.\n|\n|– themes/\n|   // Themes folder is related to an overview of the project look. (e.g. dark mode)\n|   |– _theme.scss        # Default theme\n|   |– _admin.scss        # Admin theme\n|   …                     # Etc.\n|\n|– vendors/\n|\t// All the external styles or 3rd party styles are placed in the Vendor folder.\n|   |– _bootstrap.scss    # Bootstrap\n|   |– _jquery-ui.scss    # jQuery UI\n|   …                     # Etc.\n|\n`– main.scss              # Main Sass file\n"
      })
    }), "\n", _jsx(_components.h3, {
      children: "References"
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.a, {
        href: "https://developer.mozilla.org/ko/docs/Glossary/CSS_preprocessor",
        children: "MDN - CSS 전처리기"
      })
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.a, {
        href: "https://parceljs.org/languages/sass/#example-usage",
        children: "Parcel - Sass example"
      })
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.a, {
        href: "https://13akstjq.github.io/sass/2020/02/22/mixin%EA%B3%BC-extend-%EC%95%8C%EB%A7%9E%EA%B2%8C-%EC%82%AC%EC%9A%A9%ED%95%98%EA%B8%B0.html",
        children: "[sass] mixin과 extend 알맞게 사용하기"
      })
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.a, {
        href: "https://fixmemory.tistory.com/entry/%EC%9E%85%EB%AC%B8-SASSSCSS-%EA%B7%B8%EB%A6%AC%EA%B3%A0-%EC%9E%91%EC%84%B1-%EB%B0%A9%EB%B2%95%EB%A1%A0-BEM",
        children: "입문 Sass(SCSS), 그리고 작성 방법론 BEM"
      })
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.a, {
        href: "https://andrew-barnes.medium.com/bem-and-sass-a-perfect-match-5e48d9bc3894",
        children: "BEM and SASS: A Perfect Match"
      })
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.a, {
        href: "https://www.pullrequest.com/blog/bem-and-sass-whats-the-difference-and-how-to-use-them/",
        children: "BEM and SASS? What Are They And How To Use Them"
      })
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.a, {
        href: "https://kiranworkspace.com/sass-architecture/#abstracts",
        children: "SASS Architecdture"
      })
    }), "\n", _jsx(_components.p, {
      children: _jsx(_components.a, {
        href: "https://sass-guidelin.es/#architecture",
        children: "Sass Guidelines"
      })
    })]
  });
}
export default function MDXContent(props = {}) {
  const {wrapper: MDXLayout} = props.components || ({});
  return MDXLayout ? _jsx(MDXLayout, {
    ...props,
    children: _jsx(_createMdxContent, {
      ...props
    })
  }) : _createMdxContent(props);
}
